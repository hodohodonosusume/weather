type Memo @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  horseName: String!
  date: AWSDate!
  tags: [String!]
  content: String!
}

type FavoriteHorse @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  horseName: String!
}
